# Milestone Roadmap: v0.4.0 (100% Local Polymorphic ASR Stack & SER/AED Detection)

**Status**: `COMPLETED` ✅ (2026-08-27)

---

## Completed Deliverables

### Phase 1: Modular Engine Framework & Transcriber Factory
- [x] **[ASR-005] Base Transcriber Contract**: Defined abstract `BaseTranscriber` in `src/transcribe/engines/base.py` with device auto-resolution.
- [x] **[ASR-006] Engine Registry & Dynamic Factory**: Built `EngineRegistry` and `get_transcriber()` in `src/transcribe/engines/factory.py` with lazy dynamic imports.
- [x] **[ASR-007] Faster-Whisper Polymorphic Engine**: Refactored `FasterWhisperEngine` in `src/transcribe/engines/faster_whisper.py` with full backward compatibility.

### Phase 2: Regional Indonesian & Meta MMS CTC Engines
- [x] **[ASR-008] Transformers CTC Base Engine**: Implemented `TransformersCTCEngine` in `src/transcribe/engines/transformers_ctc.py` with sliding-window chunking.
- [x] **[ASR-009] Indonesian Regional Models**: Integrated `indonesian-wav2vec2-regional` (`id`, `jv`, `su`) and `indonesian-wav2vec2-large-xlsr`.
- [x] **[ASR-010] Meta MMS Omnilingual ASR**: Integrated `facebook/mms-1b-all` with dynamic ISO-639-3 vocabulary adapter switching.

### Phase 3: Alibaba SenseVoice & SER/AED Tagging
- [x] **[ASR-011] SenseVoice-Small Engine**: Implemented `SenseVoiceEngine` in `src/transcribe/engines/sensevoice.py` supporting ~50x RTF transcription.
- [x] **[ASR-012] Emotion (SER) & Event (AED) Parsing**: Parsed `<|HAPPY|>`, `<|LAUGHTER|>`, `<|APPLAUSE|>`, `<|CRY|>`, `<|MUSIC|>` tags into structured segment attributes.
- [x] **[ASR-013] SenseVoice Local Weights Verification**: Downloaded and verified `FunAudioLLM/SenseVoiceSmall` on local server GPU/CPU.

### Phase 4: UsefulSensors Moonshine Edge ONNX Engine
- [x] **[ASR-014] Moonshine ONNX Engine**: Implemented `MoonshineEngine` in `src/transcribe/engines/moonshine.py` for zero-overhead edge execution.
- [x] **[ASR-015] Moonshine Variants**: Supported `moonshine-tiny` (27M) and `moonshine-base` (61M).
- [x] **[ASR-016] Moonshine Benchmarking**: Verified ONNX and Transformers runtime latency and RTF.

### Phase 5: Automated Model Downloader & Multi-Sample Test Suite
- [x] **[ASR-017] Unified Parallel Model Downloader**: Created `scripts/download_models.py` downloading to shared server cache (`~/.cache/huggingface/hub/`) with zero-overhead symlinks in `data/models/`.
- [x] **[ASR-018] Standardized Multilingual Test Audio**: Curated 16kHz WAV samples in `data/samples/` (ID, EN, ZH, JA, YUE) with `manifest.json`.
- [x] **[ASR-019] Full Local Weights Live Test Suite**: Created `tests/test_live_models.py` running automated real-weights verification across all model families.

### Phase 6: Universal Benchmark Matrix & Web UI Integration
- [x] **[ASR-020] Universal Benchmark Runner**: Extended `scripts/benchmark_all_models.py` and `src/transcribe/benchmarks.py` across all local backends recording WER, CER, RTF, and VRAM.
- [x] **[ASR-021] Web UI Model Selector & Badges**: Updated Web UI with multi-family dropdowns, capability badges (`[Local]`, `[Edge]`, `[SER]`, `[CTC]`), and `/api/models` endpoint.
- [x] **[ASR-022] Test Suite Integrity**: All 63 tests passing across unit, mock, and real-weights suites.
