# Milestone `v0.5.0` Roadmap (COMPLETED ✅)

> **Milestone Objective**: Deliver Universal Web Model & Variant Hierarchy Selector with Full Inference Parameter Forwarding, Automated Browser E2E Visual Verification, and Quality Issue Tracking.  
> **Status**: `COMPLETED` ✅  
> **Domain Feature Documentation**: [`../../docs/features/interface/universal-model-selector.md`](../../docs/features/interface/universal-model-selector.md)

---

## 1. Completed Tasks & Deliverables

### Phase 1: Web UI/UX Cascaded Hierarchy & Streaming Pipeline
- [x] `[WEB-001]` Extend `/api/models` endpoint with rich metadata (family, display_name, quantization options, VRAM estimates, parameter count, local cache status).
- [x] `[WEB-002]` Implement Dynamic Cascaded Selectors in Web UI (`Family` ➔ `Variant / Size` ➔ `Compute Type`).
- [x] `[WEB-003]` Implement Dynamic Adaptive Settings Panel contextually mounting model-specific knobs (Beam Size, VAD, ITN, Adapter Lang, Chunk Window).
- [x] `[WEB-004]` Implement Automatic On-Demand Server Model Download with real-time SSE progress bar feedback.
- [x] `[WEB-005]` Update Timeline Quick-Switch Pills to dynamically generate comparative 1-click re-transcribe buttons.
- [x] `[WEB-006]` Wire dynamic form parameters (`compute_type`, `beam_size`, `vad_filter`, `use_itn`, `chunk_length_s`) from Web UI into `/api/transcribe-stream`.
- [x] `[WEB-007]` Add unit and integration tests in `tests/test_server_models_selection.py` ensuring cyclomatic complexity $\le 10$.

### Phase 2: Issue Logging & Defect Tracking Protocol
- [x] `[QA-ISSUE-001]` Record defect and layout exceptions in [`../../issues/`](../../issues/).
- [x] `[QA-ISSUE-002]` Iterate on code changes and re-test until 0 errors remain.
- [x] `[QA-ISSUE-003]` Document resolved issues (Issue 001, Issue 002) in feature docs.

### Phase 3: E2E Browser Visual Verification Suite (`screenshots/e2e/`)
- [x] `[E2E-001]` Create automated Playwright E2E runner [`../../tests/e2e/test_web_models_screenshots.py`](../../tests/e2e/test_web_models_screenshots.py).
- [x] `[E2E-002]` Implement 5 standard lifecycle screenshot steps per model variant.
- [x] `[E2E-003]` Verify all 50 generated JPG screenshots across 10 model variants in 5 families.

---

## 2. Model Families Verified
1. **Faster-Whisper**: `tiny`, `base`, `small`, `turbo`.
2. **Alibaba SenseVoice**: `sensevoice-small` (FP16 / INT8).
3. **UsefulSensors Moonshine**: `moonshine-tiny`, `moonshine-base` (ONNX Edge).
4. **Meta MMS**: `meta-omnilingual-asr` (100+ languages).
5. **Indonesian CTC**: `indonesian-wav2vec2-regional`, `indonesian-wav2vec2-large-xlsr`.

---

## 3. Verification Metrics
- **Pytest Test Suite**: 70 / 70 tests passed (`100% Green`).
- **Cyclomatic Complexity**: Average CC 4.28 (Grade A), 0 Hotspots.
- **E2E Visual Captures**: 50 Screenshots in [`../../screenshots/e2e/`](../../screenshots/e2e/).
